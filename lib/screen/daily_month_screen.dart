import 'package:flutter/material.dart';

class DailyMonthScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Daily Month Screen'),
      ),
      body: Center(
        child: Text('This is the Daily Month Screen'),
      ),
    );
  }
}