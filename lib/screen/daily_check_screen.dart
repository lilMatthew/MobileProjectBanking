import 'package:flutter/material.dart';

class DailyCheckScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Daily Check Screen'),
      ),
      body: Center(
        child: Text('This is the Daily Check Screen'),
      ),
    );
  }
}